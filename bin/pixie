#!/bin/sh

# Makes pixie runnable from the repository.
dir=$(dirname "$0")/../lib/commands
if [ -d "$dir" ]
then
	AKULA_PATH=$dir
	export AKULA_PATH
fi

AKULA_COMMAND=pixie
export AKULA_COMMAND

exec akula "$@"
